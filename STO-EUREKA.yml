#Puerto por defecto
server:
  port: 8761
  
eureka:
  instance:
    hostname: localhost
  client:
    register-with-eureka: false
    fetch-registry: false
    healthcheck: 
      enabled: true
      
logging:
  level:
    com:
      netflix:
        eureka: OFF
        discovery: OFF